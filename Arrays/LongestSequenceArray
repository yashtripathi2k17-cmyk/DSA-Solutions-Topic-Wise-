class Solution {
public:
    int longestConsecutive(vector<int>& nums) {

        unordered_set<int> st;

        for (int num : nums) {
            st.insert(num);
        }

        int longest = 0;

        for (int num : st) {

            // Start only if num is the first element
            if (st.find(num - 1) == st.end()) {

                int current = num;
                int length = 1;

                while (st.find(current + 1) != st.end()) {
                    current++;
                    length++;
                }

                longest = max(longest, length);
            }
        }

        return longest;
    }
};
-------------------------------------------
Brute Force Explanation (6 Simple Steps)
Step 1

"I pick one number from the array."

Step 2

"I assume this number is the starting point of a consecutive sequence."

Example:

nums = [100,4,200,1,3,2]

Current number = 1
Step 3

"Now I check whether the next consecutive number exists."

For example

Current = 1

Check if 2 exists.

I use

linearSearch(nums, x + 1)
Step 4

"If the next number exists, I increase the sequence length and move to the next number."

1

↓

2 exists

↓

Count = 2

↓

Check 3

↓

Count = 3

↓

Check 4

↓

Count = 4
Step 5

"When the next consecutive number is not found, I stop counting and compare this sequence length with the maximum length."

Step 6

"I repeat the same process for every element and finally return the longest sequence."
class Solution {
public:
    bool linear(vector<int>&nums,int target){
       for(int x:nums){
        if(x==target){
            return true;
        }

       }
       return false;
    }
    int longestConsecutive(vector<int>& nums) {
        if(nums.empty())return 0;
        int maxi=INT_MIN;
       for(int i=0;i<nums.size();i++){
        int x=nums[i];
        int cnt=1;
        while(linear(nums,x+1)){
            x++;
            cnt++;
        }
        maxi=max(maxi,cnt);


       }
       return maxi;
    }
};


------------------------
